'use strict';

const activitypubCoreFirebaseAuth = require('..');
const assert = require('assert').strict;

assert.strictEqual(activitypubCoreFirebaseAuth(), 'Hello from activitypubCoreFirebaseAuth');
console.info("activitypubCoreFirebaseAuth tests passed");
