{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^5.6.2",
    "ts-node": "^10.9.1"
  },
  "scripts": {
    "publish": "lerna publish",
    "package": "lerna exec npm publish",
    "build": "lerna run build"
  }
}